# CryptoWatcher MVP - Инструкция

## Описание

Минимальная версия мобильного приложения для отслеживания цен криптовалют с уведомлениями. Приложение проверяет цену каждые 15 минут и отправляет push-уведомление при достижении пороговой цены.

## Возможности MVP

- ✅ Ввод тикера криптовалюты (BTC, ETH, SOL и др.)
- ✅ Установка пороговой цены в USD
- ✅ Автоматическая проверка цены каждые 15 минут (фоновые задачи)
- ✅ Push-уведомления при достижении пороговой цены
- ✅ Ручная проверка цены
- ✅ Отображение последней проверенной цены

## Установка и запуск

### 1. Установка зависимостей

```bash
cd cryptowatcher
flutter pub get
```

### 2. Проверка настроек Firebase

Убедитесь, что файлы Firebase настроены:
- `android/app/google-services.json` - должен быть на месте
- `ios/Runner/GoogleService-Info.plist` - для iOS (если нужно)

### 3. Запуск приложения

```bash
flutter run
```

## Тестирование функционала

### Тест 1: Базовая функциональность

1. **Запустите приложение**
2. **Введите тикер криптовалюты** (например: `BTC`)
3. **Введите пороговую цену**, которая уже достигнута (например: `10000` для BTC)
4. **Нажмите "Сохранить алерт"**
5. **Нажмите "Проверить цену сейчас"**
6. **Ожидаемый результат**: Должно появиться уведомление "🎯 Цена достигнута!"

### Тест 2: Проверка API CoinGecko

1. Откройте консоль разработчика (логи Flutter)
2. Сохраните алерт с любым тикером (например: `ETH`, `5000`)
3. Нажмите "Проверить цену сейчас"
4. **Проверьте логи**: Должны увидеть:
   ```
   [PriceService] Запрос цены для ETH
   [PriceService] Получена цена: $XXXX для ETH
   ```
5. **Убедитесь**, что цена отображается на экране

### Тест 3: Фоновые задачи (Workmanager)

**Важно**: Фоновые задачи работают только на реальном устройстве или эмуляторе (не на веб/десктопе).

#### Для Android:

1. **Установите приложение на устройство/эмулятор**
   ```bash
   flutter install
   ```

2. **Настройте алерт** (например: `BTC` с порогом `100000` - высокая цена)

3. **Закройте приложение** (не просто сверните, а закройте)

4. **Подождите 15 минут** или используйте команду для принудительного запуска задачи:
   ```bash
   # Для Android используйте adb
   adb shell cmd jobscheduler run -f com.example.cryptowatcher 1
   ```

5. **Проверьте логи** через:
   ```bash
   flutter logs
   # или
   adb logcat | grep -i backgroundservice
   ```

6. **Ожидаемый результат**: В логах должны появиться сообщения о проверке цены

#### Альтернативный способ тестирования фоновых задач:

1. Откройте приложение
2. Сохраните алерт с текущей ценой (например, если BTC = $50000, установите порог $49000)
3. **Измените код в `background_service.dart`** - временно уменьшите интервал до 1 минуты для тестирования:
   ```dart
   static const Duration checkInterval = Duration(minutes: 1);
   ```
4. Пересоберите и запустите приложение
5. Закройте приложение
6. Подождите 1-2 минуты
7. Проверьте уведомления

### Тест 4: Push-уведомления

#### Локальное тестирование (локальные уведомления):

1. **Запустите приложение**
2. При первом запуске должно появиться **запрос на разрешение уведомлений** - разрешите
3. Сохраните алерт с низким порогом (например: `BTC` с порогом `10000`)
4. Нажмите "Проверить цену сейчас"
5. **Ожидаемый результат**: Должно появиться уведомление в панели уведомлений Android/iOS

#### Тестирование через Firebase Console (опционально):

1. **Получите FCM токен устройства**:
   - Откройте приложение
   - В логах найдите: `[NotificationService] FCM Token: ...`
   - Скопируйте токен

2. **Отправьте тестовое уведомление через Firebase Console**:
   - Зайдите в Firebase Console → Cloud Messaging
   - Создайте новое уведомление
   - Введите заголовок и текст
   - Выберите "Отправить тестовое сообщение"
   - Вставьте FCM токен
   - Отправьте

3. **Ожидаемый результат**: Уведомление должно прийти на устройство

### Тест 5: Хранение данных

1. **Сохраните алерт** (например: `ETH` с порогом `3000`)
2. **Закройте приложение полностью**
3. **Снова откройте приложение**
4. **Ожидаемый результат**: Поля должны быть заполнены сохраненными значениями

## Проверка логов

Все действия логируются в консоль. Ключевые префиксы для поиска:

- `[Main]` - инициализация приложения
- `[PriceService]` - запросы к CoinGecko API
- `[StorageService]` - сохранение/загрузка данных
- `[NotificationService]` - работа с уведомлениями
- `[BackgroundService]` - фоновые задачи
- `[AlertProvider]` - управление состоянием
- `[HomeScreen]` - действия на главном экране

### Просмотр логов в реальном времени:

```bash
flutter logs
```

Или для Android:
```bash
adb logcat | grep -E "PriceService|BackgroundService|NotificationService"
```

## Известные ограничения MVP

1. **Фоновые задачи**: Работают нестабильно на некоторых версиях Android из-за оптимизации батареи. Решение: отключите оптимизацию батареи для приложения в настройках Android.

2. **CoinGecko API**: Бесплатный тариф ограничен 10-50 запросами в минуту. При превышении лимита запросы будут отклоняться.

3. **Тикеры**: Поддерживаются только популярные криптовалюты через маппинг. Для других монет используется тикер как ID (может не работать для всех монет).

## Устранение неполадок

### Уведомления не приходят

1. Проверьте разрешения на уведомления в настройках устройства
2. Убедитесь, что приложение не в режиме "Не беспокоить"
3. Проверьте логи на наличие ошибок `[NotificationService]`

### Фоновые задачи не работают

1. Убедитесь, что приложение установлено на реальном устройстве (не работает в эмуляторе Windows/Mac)
2. Отключите оптимизацию батареи для приложения
3. Проверьте логи на наличие ошибок `[BackgroundService]`

### API CoinGecko не отвечает

1. Проверьте интернет-соединение
2. Проверьте логи `[PriceService]` на наличие ошибок HTTP
3. Возможно, превышен лимит запросов - подождите минуту

### Ошибки компиляции

1. Убедитесь, что выполнили `flutter pub get`
2. Проверьте версию Flutter: `flutter --version` (должна быть >= 3.0.0)
3. Очистите кэш: `flutter clean && flutter pub get`

## Следующие шаги (для будущих версий)

- Добавление базы данных для истории операций
- Поддержка нескольких алертов одновременно
- Настройка интервала проверки
- Графики цен
- Портфель криптовалют
- Новости и активности

## Контакты и поддержка

При возникновении проблем проверьте логи и убедитесь, что все зависимости установлены корректно.
